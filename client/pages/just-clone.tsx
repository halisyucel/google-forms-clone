import React from 'react';
import { Helmet } from 'react-helmet';

const JustClone = () => {
	return (
		<div>
			<Helmet>
				<title>Google Forms Clone: It&apos;s Just a Clone</title>
				<meta name={'robots'} content={'noindex, nofollow'} />
			</Helmet>
			just clone
		</div>
	);
};

export default JustClone;
